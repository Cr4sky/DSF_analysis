# DSF_analysis
Utilises matplotlib and scipy to produce graphical plots and statistical analysis of differential scanning fluorimetry (DSF) data. Note that the script is generated using gemini. 

Script has flags with -f to specify the file as well as -l to specify the layout excel. Layout template is on the github page. --skip for required rows on the melt curve raw data sheet. 
